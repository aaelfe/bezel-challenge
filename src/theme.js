import { createTheme } from "@mui/material";

//assuming buttons would be reused I'd have fleshed out the theme with the other styles
let theme = createTheme({
    palette: {
        primary: {
            main: '#1a3a32'
        }
    }
})

export default theme